diff --git a/src/rime/lever/custom_settings.cc b/src/rime/lever/custom_settings.cc
index 25545d6b..21029ad8 100644
--- a/src/rime/lever/custom_settings.cc
+++ b/src/rime/lever/custom_settings.cc
@@ -18,8 +18,13 @@ static string remove_suffix(const string& input, const string& suffix) {
              : input;
 }
 
-static string custom_config_file(const string& config_id) {
-  return remove_suffix(config_id, ".schema") + ".custom.yaml";
+static path get_custom_config_path(Deployer* deployer, const string& config_id) {
+  // 创建 custom 子目录
+  path custom_dir = deployer->user_data_dir / "custom";
+  std::filesystem::create_directories(custom_dir);
+  
+  // 返回硬编码位置
+  return custom_dir / (remove_suffix(config_id, ".schema") + ".custom.yaml");
 }
 
 CustomSettings::CustomSettings(Deployer* deployer,
@@ -35,8 +40,7 @@ bool CustomSettings::Load() {
       LOG(WARNING) << "cannot find '" << config_id_ << ".yaml'.";
     }
   }
-  path custom_config_path =
-      deployer_->user_data_dir / custom_config_file(config_id_);
+  path custom_config_path = get_custom_config_path(deployer_, config_id_);
   modified_ = false;
   if (!custom_config_.LoadFromFile(custom_config_path)) {
     LOG(WARNING) << "cannot find '" << config_id_ << ".custom.yaml'"
@@ -51,8 +55,7 @@ bool CustomSettings::Save() {
     return false;
   Signature signature(generator_id_, "customization");
   signature.Sign(&custom_config_, deployer_);
-  path custom_config_path(deployer_->user_data_dir);
-  custom_config_path /= custom_config_file(config_id_);
+  path custom_config_path = get_custom_config_path(deployer_, config_id_);
   custom_config_.SaveToFile(custom_config_path);
   modified_ = false;
   return true;
@@ -84,8 +87,7 @@ bool CustomSettings::Customize(const string& key, const an<ConfigItem>& item) {
 }
 
 bool CustomSettings::IsFirstRun() {
-  path custom_config_path =
-      deployer_->user_data_dir / custom_config_file(config_id_);
+  path custom_config_path = get_custom_config_path(deployer_, config_id_);
   Config config;
   if (!config.LoadFromFile(custom_config_path))
     return true;
